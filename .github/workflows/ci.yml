name: Diagnose library clippy

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Capture diagnostics
        shell: bash
        run: |
          set +e
          cargo clippy --workspace --lib -- -D warnings > /tmp/library-clippy.log 2>&1
          status=$?
          echo "status=$status" >> /tmp/library-clippy.log
          cat /tmp/library-clippy.log
          exit 0

      - name: Upload diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: library-clippy-diagnostics
          path: /tmp/library-clippy.log
          if-no-files-found: error
