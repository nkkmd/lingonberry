name: temporary regenerate final documentation inventory

on:
  push:
    branches:
      - agent/close-final-seven-doc-blockers
    paths:
      - '.github/workflows/tmp-regenerate-final-doc-inventory.yml'

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/close-final-seven-doc-blockers
          fetch-depth: 0
      - run: python3 scripts/generate-documentation-inventory.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/DOCUMENTATION_INVENTORY.md
          git diff --cached --quiet && exit 0
          git commit -m 'docs: regenerate documentation inventory'
          git push origin HEAD:agent/close-final-seven-doc-blockers
