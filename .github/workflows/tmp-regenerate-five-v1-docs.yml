name: temporary regenerate five v1 docs inventory

on:
  push:
    branches:
      - agent/normalize-five-v1-qualification-docs
    paths:
      - '.github/workflows/tmp-regenerate-five-v1-docs.yml'

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/normalize-five-v1-qualification-docs
          fetch-depth: 0
      - run: python3 scripts/generate-documentation-inventory.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/DOCUMENTATION_INVENTORY.md
          git diff --cached --quiet && exit 0
          git commit -m 'docs: regenerate documentation inventory'
          git push origin HEAD:agent/normalize-five-v1-qualification-docs
