name: temporary regenerate publisher documentation inventory

on:
  push:
    branches:
      - agent/add-publisher-developer-quickstart
    paths:
      - '.github/workflows/tmp-regenerate-publisher-docs-inventory.yml'

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/add-publisher-developer-quickstart
          fetch-depth: 0
      - run: python3 scripts/generate-documentation-inventory.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/DOCUMENTATION_INVENTORY.md
          git diff --cached --quiet && exit 0
          git commit -m 'docs: regenerate documentation inventory'
          git push origin HEAD:agent/add-publisher-developer-quickstart
